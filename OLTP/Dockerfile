# OLTP services configuration
# MongoDB and MongoExpress are configured in docker-compose.yml at the project root
# To build additional OLTP services, add configuration here

# Example for future OLTP services that need custom image builds
